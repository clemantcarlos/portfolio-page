// Simple script to open project links in a new tab (if needed)
document.querySelectorAll('.project-link').forEach(link => {
  link.addEventListener('click', () => {
    alert('Project details coming soon!');
  });
});